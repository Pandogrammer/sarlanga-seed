+ id del button en html corresponde al nombre de function en js
    $( "#connect" ).click(function() { connect(); });

       cuando toco el boton, activa

JS:    stompClient.send("/app/hello", {}, JSON.stringify({'name': $("#name").val()}));

       le cae a

JAVA:  @MessageMapping("/hello")

       y lo devuelve en 

JAVA:  @SendTo("/topic/greetings")

       que lo ataja por estar subscripto a

JS:    stompClient.subscribe('/topic/greetings', function (greeting) {
          showGreeting(JSON.parse(greeting.body).content);
       });

       lo manda al html mediante

JS:	   function showGreeting(message) {
    	  $("#greetings").append("<tr><td>" + message + "</td></tr>");
	   }

	   que lo muestra en

HTML:  <tbody id="greetings">






//
prototipo

dungeon de 3 niveles
1 - 3 goblins
2 - 1 golem
3 - boss

pelea por turnos
+ Estado : turno jugador, turno monstruos
si turno de jugador => definir 

mostrar terreno

crear la party
10 niveles
cantidad de pjs max 4
la cantidad de niveles define los atributos
+ daño, + defensa, + vida, + skill
skills
	pegar x 2
	cd

	curar target


prototipo js html

personaje
vida
ataque
pociones


enemigo
vida
ataque


acciones
atacar
usar pocion

